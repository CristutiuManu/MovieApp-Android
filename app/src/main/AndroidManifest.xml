<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobiversal.emanuelcristutiu.movieapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".movie.MovieDetailsActivity"></activity>
        <activity
            android:name=".movies_list.HamburgerMenuActivity"
            android:label="@string/title_activity_hamburger_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".genres_list.GenresActivity" />
        <activity android:name=".actors_list.ActorsActivity" />
        <activity android:name=".PreferencesActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>